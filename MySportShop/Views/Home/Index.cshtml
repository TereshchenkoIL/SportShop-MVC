@model IEnumerable<MySportShop.Models.Product>
@{
    ViewData["Title"] = "Home Page";
}

<div class="container">

    <div class="row">
        @foreach (var prod in Model)
        {
            <partial name="_IndividualProductCard" model="prod" />
            //display all product
        }
    </div>
</div>
